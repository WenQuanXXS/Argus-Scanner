# 性能基准测试配置文件
# 定义各个测试项目的路径、语言和预期发现的漏洞类型
benchmarks:
  # Python 恶意行为模拟项目
  noriben:
    path: "test_references/Noriben"
    language: "python"
    expected_findings:
      # 预期至少发现 1 个 Base64 编码执行
      - type: "Base64 编码代码执行"
        min_count: 1
      # 预期至少发现 1 个污点数据流向敏感操作
      - type: "污点数据流向敏感操作"
        min_count: 1
  
  # Java/Go 审计工具项目
  open_audit:
    path: "test_references/Open-Audit"
    language: "python"
    expected_findings:
      # 预期发现反弹 Shell
      - type: "反弹 Shell"
        min_count: 1
      # 预期发现动态代码执行
      - type: "动态代码执行"
        min_count: 1
  
  # C 语言库 (含历史漏洞)
  libexpat:
    path: "test_references/libexpat"
    language: "c"
    expected_findings:
      # 即使未直接报 Buffer Overflow，也应有可疑条件判断作为警示
      - type: "可疑的条件判断"
        min_count: 1
      # 检测 Goto 语句
      - type: "检测到Goto语句"
        min_count: 1

  # juliet:
  #   path: "test_references/Juliet"
  #   language: "cpp"
  #   expected_findings: []
